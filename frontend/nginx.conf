server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # API is served by Traefik (same-origin /api through the edge). Do not proxy here.

    location / {
        try_files $uri /index.html;
    }
}
